import { MovieSlider } from "components";
import React from "react";

const Home = () => {
  return (
    <main>
      <section>
        <MovieSlider />
      </section>
    </main>
  );
};

export default Home;
